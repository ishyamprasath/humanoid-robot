# Copy to .env and fill in. The key is exposed to the browser — local
# robot display / kiosk use only, never a public deployment.

VITE_GEMINI_API_KEY=your-gemini-api-key-here
VITE_GEMINI_MODEL=gemini-3.1-flash-live-preview
VITE_VOICE_NAME=Kore
VITE_MODEL_FRAME_FPS=2

VITE_ULTRAVOX_API_KEY=